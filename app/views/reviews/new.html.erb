<div class="container">
  <h1>Leave a review on </h1>

  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :rating, collection: 0..5  %>
    <%= f.input :content %>
    <%= f.submit "New Review", class: "btn btn-primary"%>
  <% end %>
</div>
